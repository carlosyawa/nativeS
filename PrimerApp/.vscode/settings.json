{
    "terminal.integrated.shell.osx": "bash", "files.exclude":{
        "**//*.js" : {
            "when": "$(basename).ts"
        },
        "**/*.js.map" : {
            "when": "$(basename)"
        }
    },
    "git.ignoreLimitWarning": true
}